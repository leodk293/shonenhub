import React from 'react'

export const metadata = {
    title: 'Upcoming | Shonenhub',
    description: ''
}

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
