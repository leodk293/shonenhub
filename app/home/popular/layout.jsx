import React from 'react'

export const metadata = {
    title: 'Popular | Shonenhub',
    description: ''
}

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
