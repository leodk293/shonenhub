import React from 'react'

export const metadata = {
    title: 'Search-results | Shonenhub',
    description: ''
}

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
