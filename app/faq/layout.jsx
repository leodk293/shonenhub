import React from 'react'
export const metadata = {
    title: 'FAQ | Shonenhub',
    description: ''
}

const layout = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default layout