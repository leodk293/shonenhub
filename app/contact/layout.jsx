import React from 'react'

export const metadata = {
    title: 'Contact | Shonenhub',
    description: ''
}

const layout = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default layout