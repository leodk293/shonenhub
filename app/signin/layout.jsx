import React from 'react'

export const metadata = {
    title: 'Signin | Shonenhub',
    description: 'Login Page'
}

const layout = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default layout